{
  "name": "quvex-kalip-tests",
  "version": "1.0.0",
  "description": "Kalip ve Takim Imalati E2E Tests",
  "scripts": {
    "test:auth": "playwright test --config=playwright.config.js --project=auth-setup",
    "test:kalip": "playwright test --config=playwright.config.js --project=kalip",
    "test:all": "playwright test --config=playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
