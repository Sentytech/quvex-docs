{
  "name": "quvex-defense-cnc-tests",
  "version": "1.0.0",
  "description": "Defense CNC E2E Tests",
  "scripts": {
    "test:auth": "playwright test --config=playwright.config.js --project=auth-setup",
    "test:cnc": "playwright test --config=playwright.config.js --project=defense-cnc",
    "test:entry": "playwright test --config=playwright.config.js --project=cnc-data-entry",
    "test:perf": "playwright test --config=playwright.config.js --project=perf-timing",
    "test:all": "playwright test --config=playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
