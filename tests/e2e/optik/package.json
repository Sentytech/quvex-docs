{
  "name": "quvex-optik-tests",
  "version": "1.0.0",
  "description": "Optik Hassas Montaj E2E Tests",
  "scripts": {
    "test:auth": "playwright test --config=playwright.config.js --project=auth-setup",
    "test:optik": "playwright test --config=playwright.config.js --project=optik",
    "test:all": "playwright test --config=playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
