{
  "name": "quvex-ndt-tests",
  "version": "1.0.0",
  "description": "NDT Muayene Laboratuvari E2E Tests",
  "scripts": {
    "test:auth": "playwright test --config=playwright.config.js --project=auth-setup",
    "test:ndt": "playwright test --config=playwright.config.js --project=ndt",
    "test:all": "playwright test --config=playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
